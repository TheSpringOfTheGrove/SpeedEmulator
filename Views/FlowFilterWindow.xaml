<Window x:Class="SpeedEmulator.Views.FlowFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle, FallbackValue=筛选设置}"
        Width="590"
        Height="720"
        MinWidth="560"
        MinHeight="620"
        WindowStartupLocation="CenterOwner"
        Background="#F4F4F4">
    <Window.Resources>
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="82" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,0,12,0" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D7D7D7" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#111111" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="FilterComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D0D5D8" />
        </Style>

        <Style x:Key="FilterTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D0D5D8" />
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="18">
            <Grid Margin="0,8,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="功能区"
                           VerticalAlignment="Center"
                           FontSize="14" />
                <Border Grid.Column="1"
                        Height="1"
                        Margin="12,0,0,0"
                        Background="#DADADA"
                        VerticalAlignment="Center" />
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,54">
                <Button Content="新增"
                        Command="{Binding NewFilterCommand}"
                        Style="{StaticResource FilterButtonStyle}" />
                <Button Content="搜索"
                        Command="{Binding SearchConditionCommand}"
                        Style="{StaticResource FilterButtonStyle}" />
                <Button Content="替换"
                        Command="{Binding ReplaceTableCommand}"
                        Style="{StaticResource FilterButtonStyle}" />
                <Button Content="保存"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource FilterButtonStyle}" />
            </StackPanel>

            <Grid Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="筛选区"
                           VerticalAlignment="Center"
                           FontSize="14" />
                <Border Grid.Column="1"
                        Height="1"
                        Margin="12,0,0,0"
                        Background="#DADADA"
                        VerticalAlignment="Center" />
            </Grid>

            <ItemsControl ItemsSource="{Binding Filters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="126" />
                                <ColumnDefinition Width="116" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="66" />
                            </Grid.ColumnDefinitions>

                            <ComboBox ItemsSource="{Binding DataContext.AvailableFields, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding FieldName, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource FilterComboStyle}" />
                            <ComboBox Grid.Column="1"
                                      ItemsSource="{Binding DataContext.AvailableOperators, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding OperatorName, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource FilterComboStyle}" />
                            <TextBox Grid.Column="2"
                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource FilterTextBoxStyle}" />
                            <Button Grid.Column="3"
                                    Content="删除"
                                    Width="58"
                                    Height="30"
                                    Command="{Binding DataContext.RemoveConditionCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource FilterButtonStyle}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid Margin="0,34,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="替换区"
                           VerticalAlignment="Center"
                           FontSize="14" />
                <Border Grid.Column="1"
                        Height="1"
                        Margin="12,0,0,0"
                        Background="#DADADA"
                        VerticalAlignment="Center" />
            </Grid>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="126" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ComboBox ItemsSource="{Binding AvailableFields}"
                          SelectedItem="{Binding ReplaceFieldName, UpdateSourceTrigger=PropertyChanged}"
                          Style="{StaticResource FilterComboStyle}" />
                <TextBox Grid.Column="1"
                         Text="{Binding ReplaceSource, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FilterTextBoxStyle}" />
                <TextBox Grid.Column="2"
                         Text="{Binding ReplaceTarget, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FilterTextBoxStyle}" />

                <TextBlock Grid.Row="1"
                           Grid.ColumnSpan="3"
                           Text="{Binding StatusMessage}"
                           Foreground="#52646A"
                           Margin="0,12,0,0"
                           FontSize="12" />
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Window>
