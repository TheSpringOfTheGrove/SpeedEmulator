# 真诚公式解析与参照明细整改方案

## 1. 分析范围与结论

本次只做只读分析，没有修改业务代码、数据库或正在运行的真诚程序。

分析对象：

- 当前运行进程：`真诚财务软件.exe`，窗口标题为 `流水自动生成页面-版本(5.6.0-prod-in)-微信`。
- 项目内真诚运行时副本：`VendorRuntime/Zhencheng/真诚财务软件.dll`、`VendorRuntime/Zhencheng/caiwu-core.dll`。
- 主程序 SHA-256：`333CACF4DBB2297E0368B2A44114BD1977C1990F832846D527A81C37365306B2`。
- 核心库 SHA-256：`271EF1C446C9D4CD2D0022AAC8266ECA5AC6145319611144440D2E8EF9EAD20F`。

核心结论：

1. 真诚并不是在编辑“参照明细”时立即计算公式。参照明细先保存公式模板；生成流水时，模板字段被复制到 `GenerateFlowRecord`；流水在保存时才统一转换公式。
2. 公式只有放在“两侧各两个反斜杠”的外层定界符中才会执行。例如实际输入是 `\\(12)\\`。没有该定界符的 `(12)`、`$姓名$` 等普通文本不会执行。
3. 每个外层公式块可以混合固定文本和多个内部公式。外层定界符会被移除，不能识别为公式的内部文字会原样保留。
4. 真诚只遍历流水列中 `Type == "string"` 的字段进行转换；日期、金额等非字符串字段不直接执行公式。
5. 当前我方程序没有公式解析阶段：规则值被复制后直接保存。因此，只要参照明细里带有真诚公式，我方就可能把 `\\...\\` 原样保存进流水。

## 2. 真诚公式语法和实际语义

以下语法均应写在外层 `\\ ... \\` 中。

| 内部语法 | 含义 | 示例 |
|---|---|---|
| `(n)` | 生成 `n` 位数字 | `\\618580(13)\\` → `618580` + 13 位随机数字 |
| `{n}` | 生成 `n` 位小写字母 | `\\{8}\\` |
| `<n>` | 生成 `n` 位大写字母 | `\\<8>\\` |
| `[n]` | 生成 `n` 位数字和大写字母混合串 | `\\[16]\\` |
| `@n@` | 生成 `n` 位数字和小写字母混合串 | `\\@16@\\` |
| `#n#` | 生成 `n` 位数字、大小写字母混合串 | `\\#16#\\` |
| `%格式%` | 按流水交易时间执行 .NET `DateTime.ToString` 格式化 | `\\%yyyyMMdd HH:mm:ss%\\` |
| `$Excel列名$` | 从该 Excel 列随机抽一个值；同一流水中的不同 `$...$` 各自独立抽取 | `\\$姓$$名$\\` |
| `^Excel列名^` | 从 Excel 随机选一整行；同一流水内所有 `^...^` 共用该行 | `\\^姓名^\\` 与 `\\^卡号^\\` 保持同一行对应 |
| `&流水列名&` | 复制当前流水的另一个字段；匹配的是流水列的显示名称，不是 CLR 属性名 | `\\&卡号&\\` |

运行时探针实测：

```text
输入：prefix\\(12)-{5}-<5>-[12]-@12@-#12#-%yyyyMMdd HH:mm:ss%\\suffix
输出：prefix295431181570-damdc-QBFAA-V53Q5982Y352-5hhjtl50y6f5-WpsOdmJm5E1i-20250609 14:05:07suffix
```

由此可确认：

- 外层定界符被删除。
- 固定文本 `prefix`、`suffix` 和公式块内的连字符被保留。
- 同一公式块中的多个随机/日期公式会依次替换。
- `plain(12)` 因没有外层定界符，保持原样。
- `\\(bad)\\` 会变成 `(bad)`：外层定界符删除，但无法识别的内容保留。
- `\\(0)\\` 会变成空字符串。

## 3. 公式如何作用到参照明细

真诚的处理链路如下：

1. 参照明细或固定日期项目保存公式模板，例如对手方账号保存为 `\\618580(13)\\`。
2. 生成计划先确定交易日期、金额、规则来源和参照明细索引。
3. 按银行的 `ReferenceColumns`，把来源规则字段复制到新建的 `GenerateFlowRecord`。
4. 银行专用补位逻辑执行完成后，流水进入保存流程。
5. 保存流程遍历银行 `FlowColumns` 中的字符串字段；字段值只要包含完整的 `\\...\\` 公式块，就执行替换。
6. 替换后的结果被永久保存。因为结果中已没有外层定界符，第二次保存不会重复随机，天然具备一次性/幂等特征。

Excel 公式补充规则：

- 第一行是标题行，后续行是数据。
- `$列名$` 每出现一次都从该列非空数据中独立随机，适合不要求字段对应的随机组合；列缺失或整列无值时阻止保存。
- `^列名^` 第一次遇到时生成并写入当前流水的 `ReplaceIndex`，该流水后续所有 `^...^` 都复用此索引，用于姓名、卡号等同行关联数据；同行单元格允许为空。
- 当前流水生成对象的 `ReplaceIndex` 初始值是 `-1`，并且在真诚模型中不持久化。
- 独立 Excel 列缺失或没有非空数据时记录错误并阻止保存；同行关联 Excel 列和流水字段未找到时按兼容规则返回空内容并警告；公式块之外的文字保留。

字段复制公式补充规则：

- `&字段名&` 先按 `Bank.FlowColumns[].Name` 查找显示列名，再通过该列的 `Field` 找到 `GenerateFlowRecord` 属性并读取当前值。
- 真诚按 `FlowColumns` 顺序单遍处理，因此字段复制存在顺序依赖：被引用字段若尚未转换，读到的可能仍是原始公式文本。这是原程序的兼容性特征，也是一处可改进点。

## 4. 当前数据中的实际使用情况

对 `Data/zhencheng-flow-generation-seed.json` 做完整扫描后，当前内置规则仅在“邮政”参照明细中发现 5 个带完整外层定界符的公式值：

- 2 个对手方户名公式：使用 `$姓$`、`$名$` 从 Excel 独立抽取。
- 3 个对手方账号公式：固定前缀 `618580` 或 `618230`，后接 13 位随机数字。
- 固定日期增加项目中未发现完整公式块。
- 其他诸如商户名中的普通括号、方括号或 `^_^` 不带外层双反斜杠，因此不会被真诚公式引擎误处理。

这意味着当前最直接的用户可见问题集中在邮政规则：

- 账号可能原样显示 `\\618580(13)\\`，而不是 19 位账号。
- 姓名 Excel 数据源未读取时，`$姓$`、`$名$` 无法生成。

## 5. 我方程序的差距

### P0：缺少公式求值入口

`FlowGenerationViewModel` 生成后直接调用 `SaveAllAsync`；`InMemoryFlowRecordRepository.SaveAllAsync` 只克隆、补 ID 和序列化，没有公式转换。因此“新生成保存”和“修改后保存”都与真诚行为不一致。

### P0：随机分子 Excel 功能仍是占位实现

当前“读取Excel文件”只把状态改成“已读取”，提示仍是“入口已预留，当前使用内存样例数据”。没有读取首行标题、数据行，也没有为 `$...$`、`^...^` 提供数据源。

### P0：`ReplaceIndex` 默认值和持久化语义错误

我方 `FlowRecord.ReplaceIndex` 默认是 `0`，且会随 JSON 保存；真诚默认是 `-1`，并标记为不持久化。若直接复用真诚逻辑，我方所有 `^...^` 会固定取 Excel 第一行，且跨保存残留索引。

### P1：动态字段没有完整贯通

我方规则支持 `ExtraFields`，但适配器把规则复制到真诚记录时只复制同名 CLR 属性；从真诚记录转回我方记录时又显式跳过 `ExtraFields`。自定义流水列中的公式和值可能丢失。

### P1：公式转换按钮仍是禁用占位

流水明细页的“转换公式”按钮被禁用，命令仅显示“预留”状态。它既不能预览转换，也不能补救已有未转换流水。

### P1：缺少错误隔离和可诊断信息

真诚允许任意数字长度和任意 .NET 日期格式。过大的长度可能造成超大字符串，非法日期格式可能使保存失败；缺少银行、规则行、流水行、字段名级别的错误提示。

## 6. 整改设计

### 6.1 新增统一公式服务

新增 `IFlowFormulaEvaluator` / `FlowFormulaEvaluator`，禁止在 ViewModel、生成器、仓储中各写一套正则。

建议接口：

```csharp
FormulaEvaluationResult Evaluate(
    Bank bank,
    FlowRecord record,
    FormulaDataSet? dataSet,
    FormulaEvaluationOptions options);
```

服务职责：

- 只扫描 `Bank.FlowColumns` 中 `Type` 归一化后为 `string` 的字段。
- 同时支持 CLR 属性字段和 `[自定义字段]`/`ExtraFields`。
- 识别外层双反斜杠定界符，再解析内部 10 类公式。
- 每条流水拥有独立的计算上下文，包括随机源、交易日期、Excel 同行索引和字段快照。
- 返回错误列表、替换字段数和未解析公式数，不在底层直接弹窗。

实现上建议使用显式扫描器或有长度限制的正则，不直接对不受限输入使用可回溯的 `.*?`。

### 6.2 新增随机分子 Excel 数据服务

新增 `IFormulaDataSetService`：

- 从 `.xlsx` 第一张工作表读取数据。
- 第一行必须是唯一、非空标题；第二行起为数据。
- 以内存“行列表 + 标题索引”保存，不能只保存各列列表，否则无法实现 `^...^` 同行关联。
- 按银行隔离数据源，并在本地应用目录保存导入文件或标准化后的 JSON；重启后仍可复用。
- “清空”只清除当前银行的数据源和状态。
- 导入完成显示文件名、数据行数、标题数、重复/空标题和公式所引用但缺失的列。

兼容规则：

- `$列名$`：每个 token 从目标列非空数据中独立选择；列缺失或整列无值时阻止保存。
- `^列名^`：每条流水第一次使用时选一次行，后续复用；对应单元格允许为空。
- 缺失同行关联列或流水字段默认替换为空并记录警告；独立 Excel 公式不得静默生成空值。

### 6.3 统一保存边界

新增 `IFlowRecordSaveService` 作为所有流水保存的业务入口：

1. 克隆待保存记录，避免界面在失败时被部分修改。
2. 对所有包含外层公式定界符的字符串字段执行公式转换。
3. 校验转换后是否仍有未闭合定界符、缺失 Excel 列或超长结果。
4. 成功后调用现有 `IFlowRecordRepository.SaveAllAsync`。
5. 将转换后的值回写界面集合，保证用户看到的内容与磁盘一致。

下列保存路径必须全部改走该服务：

- 流水自动生成保存。
- 流水明细“保存全部”。
- 重新计算余额后的保存。
- Excel/PDF 导入后的保存。
- 用户合并、复制、迁移流水的保存。

这样才能符合真诚说明中的“新生成和修改后保存均生效”，并避免某个入口漏转换。

### 6.4 修正 `ReplaceIndex`

- `FlowRecord.ReplaceIndex` 初始值改为 `-1`。
- 标记 `[JsonIgnore]`，不写入流水存储。
- 每次新计算前显式重置为 `-1`。
- 更推荐把同行索引只放入 `FormulaEvaluationContext`，不要继续污染持久化模型。

### 6.5 字段复制的处理策略

为了兼容真诚，第一阶段可按 `FlowColumns` 顺序单遍转换。

随后建议增加“安全模式”：

- 先计算随机、日期和 Excel 公式。
- 再解析 `&字段名&` 依赖关系。
- 检测循环引用，如 A 引用 B、B 引用 A。
- 对循环引用和不存在字段给出行/列级错误，不产生半成品。

默认模式是否从“真诚兼容”切到“安全模式”，应以现有参照明细回归结果为准。

### 6.6 UI 调整

- 把“读取Excel文件”接到真实文件选择和校验逻辑。
- 信息按钮继续展示语法，并明确“外层必须是双反斜杠”。
- 启用“转换公式”按钮，作为预览/手动转换入口；自动保存转换仍是最终保障。
- 保存前若存在错误，集中显示：银行、规则/流水行号、字段、公式片段和原因。
- 预览时显示“转换前 → 转换后”，但不落盘，用户确认保存时才持久化。

## 7. 安全和兼容限制

- 随机长度建议限制为 `0..4096`；超限作为校验错误，避免内存放大。
- 单字段转换后长度建议限制为 16384 字符。
- 日期格式使用 `DateTime.ToString(format, CultureInfo.InvariantCulture)`，并捕获 `FormatException`。
- Excel 最大行数、列数和文件大小应设置上限；只读取首张表，不执行宏、公式或外部链接。
- 随机源通过接口注入：生产使用线程安全随机源，测试使用固定种子，确保可复现。
- 公式解析必须是一次性的：只有仍含完整外层定界符的值才执行。
- 不把原始 Excel 路径、个人数据或随机数据写入诊断日志；日志只记列名、行号和错误类别。

## 8. 分阶段实施建议

### 第一阶段：P0，恢复核心兼容性

1. 实现 6 类随机公式和日期公式。
2. 接入生成保存、流水明细保存两个主路径。
3. 修正 `ReplaceIndex = -1` 和不持久化。
4. 用邮政的 `\\618580(13)\\` 做端到端回归。

完成标准：新生成邮政流水中不再出现 `\\(13)\\`，账号为 `618580/618230 + 13 位数字`。

### 第二阶段：P0/P1，补齐 Excel 和关联字段

1. 实现真实 Excel 导入、按银行缓存和清空。
2. 实现 `$列名$` 和 `^列名^`。
3. 实现 `&流水列名&`。
4. 支持 `ExtraFields`。

完成标准：姓名/卡号的 `^...^` 始终来自同一 Excel 行并允许同行单元格为空；`$...$` 只从目标列非空数据中独立抽取，缺失列或整列无值时阻止保存。

### 第三阶段：P1/P2，补齐产品体验和迁移

1. 启用“转换公式”预览。
2. 扫描本地历史流水中的未转换公式，生成只读报告。
3. 用户确认后再批量转换历史数据，并先做备份。
4. 增加循环引用、长度上限、格式错误和性能测试。

## 9. 必须通过的验收用例

1. `plain(12)` 保存后仍为 `plain(12)`。
2. `\\ABC(4)XYZ\\` 保存后为 `ABC` + 4 位数字 + `XYZ`。
3. 一个字段包含两个外层公式块时，两块均转换。
4. `%yyyyMMdd%` 使用该流水的 `AccountTime`，不是系统当前时间。
5. `$姓$$名$` 可以来自不同 Excel 行。
6. `^姓名^` 与 `^卡号^` 在同一流水内必须来自同一行，不同流水可选不同的行。
7. `&卡号&` 能读取强类型字段；`&自定义列&` 能读取 `ExtraFields`。
8. 独立 Excel 公式不允许生成空值；同行关联 Excel 列和缺失流水列按兼容规则替换为空并输出可定位警告。
9. 同一批数据保存第二次，结果完全不变。
10. 自动生成保存、手工修改保存、导入保存、重算保存和合并保存行为一致。
11. 邮政内置 5 个公式值全部不再以原始 `\\...\\` 形式出现在保存结果中。
12. 全银行回归中，普通括号、方括号、`^_^` 等非外层公式文本保持不变。

## 10. 预计改动位置

- 新增：`Services/FlowFormulaEvaluator.cs`
- 新增：`Services/FormulaDataSetService.cs`
- 新增：`Services/FlowRecordSaveService.cs`
- 修改：`Models/FlowRecord.cs`
- 修改：`ViewModels/FlowGenerationViewModel.cs`
- 修改：`ViewModels/FlowDetailsViewModel.cs`
- 修改：`Views/FlowDetailsWindow.xaml`
- 修改：所有直接调用 `IFlowRecordRepository.SaveAllAsync` 的业务入口
- 测试：新增公式单元测试、Excel 同行关联测试、邮政种子端到端测试和保存幂等测试

不建议直接调用真诚的混淆私有公式类作为正式实现。该类名和方法名已经混淆，版本升级容易失效；我方应按已确认语义实现稳定、可测试的本地公式引擎。
